#!/usr/bin/env node

const ActureMcpServer = require('../src/server/index.js');

// Start the MCP server
const server = new ActureMcpServer();
server.run().catch((error) => {
  console.error('Fatal error running MCP server:', error);
  process.exit(1);
});
